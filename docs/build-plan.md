# 제작 진행 파일 — 게이트 5 (방 21개)

> 🔴 **매 회차 시작할 때 이 파일부터 읽는다.** 하나만 하고, 끝나면 아래 표와 기록을 갱신한다.
> 회차 사이에 대화 맥락이 날아가도 이 파일만 있으면 이어갈 수 있다.
> 시작 2026-08-30 · 사장님 지시로 1시간에 한 회차씩 · **토큰을 나눠 쓴다**

## 지금 어디까지

- 게이트 4 통과 (친구 검증 — *"재밌다"*)
- 설계 연구 2 완료 → `docs/map-study.html`
- 규칙 확정: 요소 ③(몸을 두고 핵만 간다) · 문벽 · 문 여러 개
- 🔴 **두고 온 몸이 디딤돌이다** (08-30 확인). 문을 열면 그 자리가 영구 지형이 된다

## 🔴 회차 규칙

- **한 회차에 하나만.** 다 하려 들지 말 것 — 토큰을 나눠 쓰는 게 지시다
- 회차 끝에 **반드시** 이 파일의 표와 기록을 갱신한다
- 코드를 고쳤으면 **컴파일 + 정합성 검사**까지 하고 끝낸다 (Unity가 열려 있으면 그렇다고 적는다)
- 판을 만들었으면 **솔버 검증**을 통과한 것만 넣는다 (풀림 · 최단해 1개)
- 🔴 **새 규칙을 만들지 않는다.** 게이트 5는 "새 시스템 금지"다.
  꼭 필요하면 §7 절차를 적어두고 **사장님께 묻는다** — 혼자 넣지 않는다
- 막히면 **다음 회차로 넘기지 말고** 왜 막혔는지 적는다

## 확정된 설계 (설계 연구 2)

```
모양     큰 고리 + 짧은 곁가지(2~3방) · 21방
잠금     통로의 20~30%만. 막다른 길엔 안 건다
이동     방 사이는 그냥 걸어서. 🔴 오르막은 두고 온 몸(디딤돌)으로 넘는다
조각     문이 있는 방에. 통로엔 최소한만
성장     방마다 만들 수 있는 최대 길이가 다르다 (조각 수가 정한다)
능력     하나만 — 머리 바꾸기. 고리 반대편에. 얻으면 핵 색이 바뀐다
엔딩     곁가지 끝 = 문양 한 획 5개 · 마지막 문 · 마지막 방엔 심이 없다
길찾기   두고 온 몸 + 구역 색. 지도는 안 만든다
```

## 🔴 분량 목표 (2026-08-30 사장님 답)

> *"뺄 생각은 없는데 최대한.. 안 되면 어쩔 수 없고"*

**2시간은 목표에서 안 뺀다.** 다만 순서를 이렇게 잡는다:

1. **21방을 먼저 완성한다** — 이게 최종이 아니라 **첫 측정 지점**이다
2. 처음부터 끝까지 돌려 **실제 시간을 잰다** (기록 장치가 이미 있다)
3. 모자란 만큼 **고리에 덧붙인다** — 곁가지를 더 뻗거나 옆에 새 고리를 붙인다

🔴 지금 계산(40~60분)은 어제 사장님 기록(판당 평균 20초)으로 뽑은 것이라
실제로 만들어 재보면 달라질 수 있다. **재기 전에 늘리지 않는다.**

## 회차 계획

| 회차 | 할 일 | 상태 |
|---|---|---|
| 1 | **C# 엔진에 디딤돌 반영** — JS에만 넣었다. 두 엔진이 어긋나면 안 된다. 컴파일+정합성까지 | ✅ |
| 2 | **화면에 굳은 몸을 제대로** — 지금은 색만 바뀐다. 디딜 수 있다는 게 보여야 한다 | ✅ |
| 3 | **구역 생성기 다시** — 평지 구역. 곁가지는 보류 | ✅ |
| 4 | **첫 구역을 게임에 넣기** | ✅ |
| 4b | **곁가지(높은 선반) 다시** | ✅ |
| 5 | **머리 바꾸기(요소 ②)** — 🟡 **§7 준비 완료. 사장님 답 대기** | 🟡 |
| 6 | **둘째 구역** | ✅ |
| 6b | **COLS=4가 안 풀리던 원인** — 선반이 문보다 좁았다 | ✅ |
| 7 | **문양 획** — 모으고 보여주기 | ✅ |
| 8 | **구역 5개 + 마지막 방** | ✅ |
| 9 | **21방 채우기** | ✅ |
| 10 | **전체 한 번 돌려보기** — 이어지는지 검사 | ✅ |
| 11 | **WebGL 빌드 + 사장님 확인** — 🔴 실제 시간은 여기서만 잴 수 있다 | ⬜ |
| 12 | 🔴 **분량 늘리기** — 어림 13분. 목표 2시간과 9배 차이. 아래 진단 참고 | ⬜ |

## 회차 기록

(회차를 끝낼 때마다 여기에 한두 줄. 막혔으면 왜 막혔는지도)

**1회차 (08-30 03:2x)** — C# 엔진에 Level.IsSpent() 를 넣고 Supported() 가 그걸 보게 했다.
두고 온 몸 위에 설 수 있다. 컴파일 통과 · 정합성 검사 통과(판 13개, C#이 JS와 같은 답).
JS 쪽은 이미 들어가 있었고, 디딤돌이 실제로 길을 여는 것도 시험판으로 확인했다 —
문2의 발밑이 전부 문1 자리라 **문1을 열기 전엔 앉을 수조차 없는** 판이 15걸음에 풀린다.

**2회차 (08-30)** — 굳은 몸을 **홈이 아니라 지형으로** 그린다.
색을 벽(Rock) 계열로 바꾸고(0x3a4a43), 테두리도 같은 돌색이 되게 했다 — 민트 홈이 아니라 돌이 된 것이다.
🔴 그리고 **윗면**(0x667d72)을 얹었다. 벽 윗면과 같은 신호라 "여기 설 수 있다"가 바로 읽힌다.
윗면은 **덩어리의 맨 위 칸에만** 켠다 — 속에까지 그리면 줄무늬가 된다.
컴파일 통과 · 정합성 통과.
⚠️ 눈으로는 확인 못 했다. Play에서 문을 열어보면 굳은 몸에 밝은 윗면이 생겨야 한다.

**3회차 (08-30)** — 평지 구역 하나가 **65걸음, 최단해 1개**로 풀린다 (tools/region.json).
방 셋을 한 줄로 놓고 **바닥 높이 통로**로 이었다. 시작이 가운데라 좌우로 자유롭고,
오른쪽은 문벽이 막아 왼쪽 방의 문을 열어야 간다.

🔴 배운 것 셋 (전부 못 풀리는 판을 만들고 알았다):
1. **길이 1인 핵은 계단도 못 오른다** (k칸 = 길이 k+1, k=1도 길이 2) → 통로는 전부 평지
2. **이동이 조각을 소비하면 안 된다** — 조각은 문이 요구하는 만큼만이라,
   위층에 오르려고 먹으면 문을 못 채운다 → 🔴 **곁가지(위층)를 보류. 4b회차로**
3. **문은 지형에 붙어 있어야 한다** — 공중에 뜬 문은 그 방 조각으로 못 닿는다
   (바닥에서 5칸 위에 놓여 길이 4로는 못 가는 판이 나왔다).
   생성기가 이제 "칸마다 아래가 벽이거나 같은 문의 칸"만 놓는다

**4회차 (08-30)** — 첫 구역 r1을 levels.json 맨 앞에 넣었다. **65걸음 · 최단해 1개 · C#이 JS와 같은 답.**

🔴 넣자마자 **두 엔진이 어긋난 걸 정합성 검사가 잡았다.**
JS는 clear가 "all"이면 문 크기가 달라도 되게 고쳤는데 C#은 무조건 막고 있었다
("문마다 칸 수가 다르다 3 vs 4"). C#도 Clear를 보게 맞췄다.
→ 이래서 정합성 검사가 있다. 한쪽만 고치면 조용히 어긋난다.

정답 수순을 보면 구조가 그대로 읽힌다 —
가운데에서 왼쪽 끝까지 걸어가 문1을 채우고(문벽이 열린다), 오른쪽 끝까지 쭉 걸어가 문2를 채운다.
🔴 **가운데 왕복이 통째로 이동 시간**이다. 퍼즐이 아니라 걸어다니는 시간이고, 그게 이 구조가 노린 것.

⚠️ Play로는 아직 안 봤다. 방 단위 카메라·문벽 사라짐·굳은 몸 윗면이 실제로 어떻게 보이는지는 확인이 필요하다.

**4b회차 (08-30)** — 곁가지가 된다. 구역이 **75걸음 · 최단해 1개 · 상태 20,862**로 깊어졌다(지난 것의 28배).

🔴 3회차에서 막혔던 게 **문제를 잘못 본 것**이었다. "이동이 조각을 소비한다"가 아니라
**조각이 그 문과 같은 방에 있을 필요가 없다**는 걸 놓친 것이다.
가운데 방 바닥에서 조각 3개를 먹고 → 길어진 채로 선반에 올라 문2를 채우고 →
길이 1로 떨어져 → 왼쪽 방에서 조각 2개로 문1. **오르는 건 길이가 필요하고 내려오는 건 공짜다.**

🔴 규칙을 하나 더 배웠다 (또 못 풀리는 판을 만들고):
**문 칸 수가 n이면 바닥에서 n-1칸 위까지만** 놓을 수 있다.
채울 때 길이는 정확히 n이고 위로 k칸 = 길이 k+1이므로.
3회차의 "지형에 붙어야 한다"만으로는 부족했다 — **선반 위에 붙었는데 그 선반을 못 올라가는 판**이 나왔다.
붙어 있는 것과 닿는 것은 다르다.

---

## 🔴 §7 개정 절차 — 머리 바꾸기(요소 ②)  [사장님 답 대기]

**넣자는 것:** 키 하나로 **머리와 꼬리를 맞바꾼다.** 한 걸음을 쓴다.

**버릴 것 (셋):**

1. **게이트 5의 "새 시스템 금지"** — 이 단계는 분량만 채우기로 했다. 규칙이 하나 는다
2. 🔴 **지금 판 11개 중 8개의 난이도** — 측정됨(`tools/exp-swap.js`):
   1-5는 38→26걸음, b3는 42→33, b6는 33→26, b1은 35→29. **3~12걸음씩 짧아진다**
3. 🔴 **채택 기준 하나** — b2는 최단해가 1개 → 2개가 된다. *"최단해가 하나뿐일 것"* 이 깨진다

**넣자는 이유:**

- 🟢 **책상이 아니라 손에서 나왔다.** 사장님이 b1에서 막혔을 때 *"머리 바꾸는 키가 있어?"* 라고 물으셨다.
  그때 막힌 입력이 26회였다 — 몸에 가로막혀 못 간 것이다
- 🟢 설계 연구 1·2가 둘 다 이걸 전제로 짜여 있다 (능력은 하나만 · 고리 반대편에)
- 🟢 중력과 안 부딪힌다 — 뒤집어도 칸이 안 움직여서 안 떨어진다
- 🟢 상태 공간이 1.0~2.7배만 는다 (몸 길이는 칸당 3.6배인데 비하면 아주 싸다)
- 🟢 §6이 금지한 **"넷째 요소"가 아니다.** 설계 연구 때부터 비워둔 칸이다

**안 넣어도 되는 이유:**

- 🔴 **지금은 아직 필요 없다.** 설계상 능력은 *"고리 반대편"* 에 두는데, 구역이 하나뿐이다.
  구역이 서넛 생긴 뒤에 넣어도 늦지 않고, 그때가 배치를 정하기도 쉽다
- 🔴 넣으면 기존 판을 다시 검증해야 한다 — 지금 하면 두 번 하게 된다

**백업:** 이미 있다 (`git push` 완료, `origin/master`)

### 🔴 물을 것

> **머리 바꾸기를 지금 넣을까, 구역이 서넛 생긴 뒤에 넣을까?**
>
> - **지금** → 다음 회차에서 엔진 양쪽 + 키 + 핵 색 바뀜까지. 기존 판 11개 재검증
> - **나중** → 6회차(둘째 구역)로 넘어간다. 능력 없이 만들고, 나중에 능력 구간을 뒤에 붙인다

제 생각엔 **나중**이다. 지금 넣으면 판을 두 번 검증하게 되고, 능력이 열어줄 곳이 아직 없다.

**6회차 (08-30)** — 둘째 구역 r2. **35걸음 · 최단해 1개 · 상태 39,211.**
🔴 첫 구역(75걸음/20,862)보다 **걸음은 짧은데 상태가 두 배**다 — 손은 짧고 생각은 길다.
모양도 다르다: 첫 구역은 문 둘이 양 끝에 있고, r2는 **왼쪽 방에 문 둘이 겹쳐** 있다.
r1 → r2 로 이었다. 생성기에 COLS·D1·D2·LEDGE 를 열어 구역마다 다르게 뽑는다.

막힌 것 · 미룬 것:
- 🔴 **COLS=4가 하나도 안 풀린다** (12개 씨앗 × 여러 문 크기 전부 실패). 원인 미상 → **6b회차**
- 장식 선반이 문 자리를 덮고 있었다 → 문 자리를 먼저 비우고 놓게 고침

**6b회차 (08-30)** — 🔴 원인은 **선반이 문보다 좁았던 것**이었다.



문의 심이 오른쪽 끝이라 **왼쪽에서 걸어 들어가야 하는데** 그 자리가 허공이었다.
COLS=3에서는 우연히 자리가 맞아 가끔 풀렸던 것이다.
→ 선반을 문보다 **양쪽으로 한 칸씩 넓게** 깐다.

효과: 성공률이 확 올랐다. COLS=3은 씨앗 14개 중 6개, **COLS=4는 6개 중 4개**.
전엔 여섯에 하나 나올까 말까였다.

두 구역을 새 생성기로 다시 뽑았다 — r1 **78걸음**(방 3개), r2 **43걸음**(방 4개).
정합성 통과.

🔴 배운 것: **문은 들어가는 쪽에 설 자리가 있어야 한다.**
지금까지 문 조건이 셋으로 늘었다 — 지형에 붙을 것 · 바닥에서 n-1칸 이내 · **드나들 자리가 있을 것**.

**7회차 (08-30)** — 문양 획을 모으고 보여준다. 화면 아래에 ◆ ◆ ◇ ◇ ◇ 로 뜬다.

🔴 **새 규칙을 안 만들었다.** 이미 있는 진행 저장(깬 판 목록)에서 세기만 한다 —
판마다 mark: true 를 달면 그 구역이 획을 준다. 따로 저장하는 값이 없다.

마지막 문 조건(획 5개)은 **8회차로 미뤘다.** 구역이 둘뿐이라 지금 만들면 시험할 수가 없다.
그때 셋째~다섯째 구역을 같이 만든다.

폰트에 ◆◇ 가 필요해서 다시 잘랐다 (tools/subset-font.py).

**8회차 (08-30)** — 구역 다섯 + 마지막 방. **78 · 63 · 60 · 67 · 47걸음 + 27걸음.** 정합성 통과.

- 🔴 **마지막 방엔 심이 없다.** 어디서 끝내도 되지만 모양은 정확해야 한다 (ㄹ자로 꺾인 6칸).
  규칙을 더하는 게 아니라 **빼는** 엔딩 — 설계 연구 2의 7회차 결론 그대로
- **마지막 문에 needMarks: 5** — 획이 모자라면 안 열리고 *"표식이 모자라다"* 가 뜬다.
  새 규칙이 아니라 **길을 막는 것**이고, 깬 판 목록에서 세기만 한다

🔴 **도구 버그를 하나 잡았다.** 생성기가 "해가 없음"일 때도 **종료코드 0**을 냈다.
그래서 실패한 판을 복사할 때 **이전 판이 그대로 복사**됐고, r2가 r3로 r4가 r5로 베껴졌다.
정합성 검사에 정답 수순이 똑같이 찍혀서 알아챘다.
→ 못 풀리면 exit(1). 그리고 조립할 때 **겹치는 구역이 있으면 멈추는 검사**를 넣었다.

> 성공한 것만 세면 안 된다. **실패가 실패로 보여야** 한다.

**9회차 (08-30)** — 🔴 **21방.** 게이트 5의 조건을 채웠다.

```
r1  3방 78걸음   r4  4방 67걸음
r2  5방 56걸음   r5  4방 47걸음
r3  4방 60걸음   fin 1방 27걸음      합계 21방 · 335걸음
```

방 수는 구역의 폭에서 나온다 (방 폭 11 + 벽 1). 19방이라 둘이 모자랐고,
r2를 **방 다섯짜리**로 바꿔 채웠다. 구역을 새로 붙이는 것보다 획 개수가 안 흔들려서 낫다.
(COLS=2짜리 작은 구역도 시도했으나 하나도 안 나왔다 — 문 둘이 한 방에 몰리면 자리가 안 난다)

겹치는 구역이 없는지 조립할 때마다 확인한다 (8회차 사고 이후).
정합성 통과.

⚠️ **아직 사람이 처음부터 끝까지 해본 적이 없다.** 335걸음이 몇 분인지는 10회차에서 잰다.

**10회차 (08-30)** — `tools/playthrough.js` 로 처음부터 끝까지 걸어봤다. **🟢 이어진다.**

```
r1 78 › r2 56 › r3 60 › r4 67 › r5 47 › fin 27      21방 · 335걸음 · 획 5개
```

획이 제때 모여 마지막 문이 열린다. 링크가 끊긴 데도, 고리에 갇히는 데도 없다.
판 하나씩 보는 stamp로는 못 잡는 것들이라 이 도구를 따로 만들었다.

## 🔴 어림 13분 — 목표 2시간과 9배 차이

걸음당 0.9초 · 최단의 2.5배를 헤맨다고 잡은 값이다(어제 사장님 기록에서).
처음 하는 사람이라도 20~30분이지 2시간은 아니다.

설계 연구 7회차가 시간이 나올 데를 넷으로 봤는데, 지금 상태를 대보면:

| 갈래 | 지금 |
|---|---|
| 방을 더 만든다 | 21방. 늘릴 수 있지만 선형 |
| 방을 더 어렵게 | 🔴 **거의 안 했다** — 생성기가 뽑은 것 그대로 쓴다 |
| 한 방에 문 여럿 | 🟡 구역마다 문 둘뿐 |
| 길 잃고 돌아다니기 | 🔴 **거의 없다** — 구역이 한 줄이라 헤맬 데가 없다 |

🔴 **셋째·넷째가 통째로 비어 있다.** 지금 구역은 "왼쪽 갔다 오른쪽 가는 한 줄"이고,
설계 연구가 그렸던 **고리와 곁가지**가 아니다.
3회차에서 "길이 1은 못 오른다" 때문에 평지로 눕혀놓고 그대로 왔다.

→ 12회차에서 다룬다. 다만 **실제 시간을 재기 전에는 늘리지 않는다**(사장님 결정, 08-30).
