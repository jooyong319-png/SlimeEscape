# ⚠ 이 폰트는 배포하면 안 된다

`kr.ttf` 는 윈도우 기본 글꼴(`malgun.ttf`) 사본이다. **재배포 불가**다.

지금 있는 이유: WebGL 빌드에서 한글이 전부 네모로 깨져서, 친구 몇 명에게
비공개로 보여줄 테스트 빌드를 급히 만들어야 했다 (2026-08-29).

🔴 **itch.io에 공개로 올리기 전에 자유 라이선스(SIL OFL) 한글 폰트로 갈아야 한다.**
   후보: 나눔고딕 · Pretendard · Noto Sans KR
   파일 이름만 `kr.ttf` 로 맞춰 덮으면 코드는 안 고쳐도 된다.

원인과 일반적인 해법은 통합 위키 `unity.md` 의
"WebGL 빌드하면 한글이 다 깨진다" 항목에 있다.
